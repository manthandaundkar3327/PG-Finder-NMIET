# Backend

From this folder run:

```powershell
npm install
npm start
```

The server uses the SQLite database located at:
`../frontend/campusnest.db`

The frontend is served from:
`../frontend/public`
