# PG's Finder v12

## Project structure
- `frontend/` — browser-side HTML, CSS, JavaScript and assets.
- `backend/` — server-side/API files and backend configuration.

## Paths
References between files were updated to match their new folder locations.
Frontend assets use paths relative to the HTML/CSS/JS file location, while backend
files use paths relative to the backend file location.
